<Page x:Class="ForecastDesign.Views.ViewPages.ViewRegistration"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ForecastDesign.Views.ViewPages" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      Name="page"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ViewRegistration">

    <Border  Style="{DynamicResource BorderBackround}"  CornerRadius="30">
        <Grid x:Name="grid" Background="Transparent">
            <StackPanel x:Name="stackpanel" Grid.Row="1" Background="Transparent" VerticalAlignment="Center">
                <Grid VerticalAlignment="Center" Width="{Binding ElementName=border, Path=Width}" Grid.Row="0" Margin="0,5,0,0">
                    <Border Height="70" BorderThickness="0" Width="300" CornerRadius="20" x:Name="border" Style="{DynamicResource borderStyle}">
                        <Border.Background>
                            <LinearGradientBrush>
                                <GradientStop Color="Black" Offset="0.2"/>
                                <GradientStop Color="AliceBlue" Offset="0.0"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                    <Label FontSize="30" Foreground="Gray" Height="70" Width="300" HorizontalContentAlignment="Center" Content="Gmail" Style="{DynamicResource LabelStyle}"/>
                    <TextBox Name="textbox" FontSize="30" Foreground="Gray" Height="70" Width="300"  Style="{DynamicResource TextBoxStyle}" />
                </Grid>
                <Button CommandParameter="{Binding ElementName=page}" Command="{Binding SendCodeCommand}" Style="{DynamicResource ViewInButtonStyle}" Content="Send Code" Margin="20"/>
                <Label Visibility="Hidden" Height="40" Width="300" Content="errrorere" FontSize="20" HorizontalContentAlignment="Center" Foreground="red"/>

            </StackPanel>
        </Grid>
    </Border>
</Page>
